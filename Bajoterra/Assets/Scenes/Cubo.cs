using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cubo : MonoBehaviour
{
    /*public Transform cubo;
    public Vector3 posFinal;
    [SerializeField] private float velocidad; //SerializeField muestra una variable privada en el inspector

    void Start()
    {
        cubo = GetComponent<Transform>();
        //Vector3 = (x, y, z);
    }

    void Update()
    {
        cubo.position = Vector3.Lerp(cubo.position, posFinal, Time.deltaTime * velocidad);
    }*/
}
